{% extends 'base.html.twig' %}

{% block title %}Flipbook Hébémag{% endblock %}

{% block styles %}
{% endblock %}

{% block body %}
 
    
{% endblock %}

                {# <object src="{{ asset('uploads/ebooks/' ~ ebooks.pdf) }}" type="application/pdf"  /> #}

    {# <object type="application/pdf" data="{{ asset('uploads/ebook/' ~ ebooks.pdf) }}" width="100%" height="100%"></object> #}
         {# {% for image in images %}
            <div><img src="{{ asset(image) }}" alt="Page {{ loop.index }}"></div>
        {% endfor %} #}